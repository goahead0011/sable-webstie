# sable MVP Context Snapshot

- Task statement: Build an MVP for an editorial/select-shop website named `sable`, but do not implement yet.
- Desired outcome: Analyze the supplied Figma references and requirements, then produce clarifying questions and an implementation plan.
- Context type: Greenfield. `/Users/fredogden/Desktop/sable-website` currently has no app source files and is not a Git repository.
- Prompt-safe summary status: not_needed.

## Known Requirements

- Site name: `sable`.
- Menu: Home, Brands, New in, Women, Men, Life, Sale, Styling, Magazine, Information, Search, Login, Cart.
- Figma may say `man`; implementation must use `Men`.
- MVP product images should be gray placeholders.
- No real payment implementation.
- Login, Cart, Checkout can be mock screens.
- Responsive support is required for desktop, tablet, and mobile.
- Tablet/mobile designs are not supplied, so preserve the desktop tone and propose a reasonable responsive structure.

## Figma Findings

- Desktop frame width appears to be 1280px.
- Header is minimal, about 69px tall on Home/Product pages and 86px in the search overlay reference.
- Main background is near-white `#fbfbfb`; text is near-black `#191919`.
- Typography uses Pretendard, regular/bold, mostly 14px.
- Home hero/video placeholder begins below the header and uses light gray `#d9d9d9`.
- Brands hover modal is a white dropdown panel from the header area, about 230px tall, with a five-column brand list.
- Product grid desktop uses 3 columns, 320px wide cards, 400px image area, 20px horizontal gutter, and about 16px vertical image gap in default state.
- Product hover reveals product/brand and price below the image; the hovered card height increases and pushes the next row down.
- Search overlay uses a white top search bar with close/search icons and a dimmed page backdrop.

## Open Questions

- Preferred frontend stack and styling approach are not specified.
- Product taxonomy and whether `style` from the reference means one shared listing type or category filters is not specified.
- Brand detail page content depth is not specified.
- Styling page detail behavior is not fully specified.
- Magazine and Information page MVP depth is not fully specified.
- Cart mock behavior and persistence expectations are not specified.

## Likely Touchpoints

- App routing.
- Shared navigation/header.
- Brand dropdown/mega menu.
- Search overlay and search result page.
- Shared product grid and product card hover behavior.
- Product detail page.
- Local data files for products, brands, and articles.
