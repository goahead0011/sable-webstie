{
  "task": "Fix sable desktop Brands mega-menu (hover->pinned state model, single DOM instance) + SearchOverlay focus trap/return; sequential ralph execution",
  "stories": [
    {
      "id": "US-000",
      "title": "Step -1: verify symptom + backup",
      "priority": 1,
      "passes": true,
      "acceptanceCriteria": [
        "Files to be modified are backed up (non-git repo) under _backup/",
        "dev server runs and root page renders",
        "Determined whether the reported 'duplicate modal' is real duplicate DOM or a Link-navigate illusion, with evidence from the rendered markup/source"
      ]
    },
    {
      "id": "US-001",
      "title": "BrandMegaMenu component with useReducer {visible,pinned}",
      "priority": 2,
      "passes": true,
      "acceptanceCriteria": [
        "New components/layout/BrandMegaMenu.tsx is a 'use client' component owning its own state via useReducer with {visible, pinned}",
        "Single DOM instance: exactly one panel node rendered by Header",
        "Trigger is a <button> with aria-haspopup, aria-expanded, aria-controls; panel is <nav aria-label='Brands'> with link list (NOT role='menu')",
        "hover opens (visible); click pins; click again closes; pinned ignores mouseleave; outside-click closes; Escape closes and returns focus to trigger",
        "outside-click listener early-returns when panelRef/triggerRef contains target; brand links navigate to /brands/[slug]; 'All brands' link navigates to /brands",
        "pointer-leave uses ~120ms timeout (ref-stored, cleared on enter/pin/navigate/unmount); focus-out closes synchronously",
        "keyboard: focus moves into panel on pin; brand links are tabbable"
      ]
    },
    {
      "id": "US-002",
      "title": "Header rewrite wiring single BrandMegaMenu",
      "priority": 3,
      "passes": true,
      "acceptanceCriteria": [
        "Header no longer holds brandsOpen boolean / inline mega-menu markup",
        "Brands desktop trigger no longer a navigating <Link>; navigation relocated into the panel's 'All brands' link",
        "Mobile drawer Brands accordion (tap open) preserved",
        "No second mega-menu DOM rendered anywhere"
      ]
    },
    {
      "id": "US-003",
      "title": "SearchOverlay focus trap + focus return",
      "priority": 4,
      "passes": true,
      "acceptanceCriteria": [
        "Tab cycles within the overlay dialog (focus trap)",
        "On close, focus returns to the Search trigger",
        "Enter/submit still routes to /search?q= (existing behavior preserved)",
        "Body scroll lock remains single-owned (no double management)"
      ]
    },
    {
      "id": "US-004",
      "title": "Product hover push-down preserved",
      "priority": 5,
      "passes": true,
      "acceptanceCriteria": [
        "Existing max-height normal-flow reveal kept (no grid-rows rewrite)",
        "Desktop hover/focus expands metadata pushing rows below down",
        "Mobile tap-to-expand retained"
      ]
    },
    {
      "id": "US-005",
      "title": "lint + build green",
      "priority": 6,
      "passes": true,
      "acceptanceCriteria": [
        "npm run lint passes with 0 warnings/errors",
        "npm run build succeeds"
      ]
    }
  ]
}