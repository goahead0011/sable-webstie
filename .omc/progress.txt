RALPH SESSION — sable Brands mega-menu fix + SearchOverlay a11y (2026-05-23)

STEP -1 RESULT:
- Verified via rendered SSR HTML: exactly ONE brandMega container existed (1 container + 1 inner + 29 links).
- The "duplicate modal" was a Link-navigate ILLUSION: Brands trigger was <a href="/brands">; clicking navigated to /brands (BrandDirectory grid) while the hover mega-menu was still visible -> looked like a 2nd modal.
- ROOT CAUSE: trigger was a navigating <Link>, and state was a single boolean with no pin concept. NOT duplicate DOM.
- Proceeded with hover->pinned state-model refactor (no de-duplication needed). Backups in _backup/.

IMPLEMENTATION:
- NEW components/layout/BrandMegaMenu.tsx: "use client", owns useReducer({visible,pinned}). isOpen=visible||pinned.
  Events: POINTER_ENTER, HOVER_CLOSE(120ms, ignored if pinned), TOGGLE_PIN(click; pin or full-close), OUTSIDE, ESCAPE(+focus return), FOCUS_OUT(sync), NAVIGATE.
  Trigger=<button aria-haspopup/aria-expanded/aria-controls=brand-mega>. Panel=<nav id=brand-mega aria-label=Brands inert when closed>. "All brands"->/brands inside panel.
  Outside-click listener early-returns on trigger/panel contains. No onFocus on trigger (prevents Escape-reopen). Timer ref cleared on enter/pin/navigate/unmount.
- Header.tsx: removed brandsOpen + inline mega + handleHeaderBlur. Renders single <BrandMegaMenu triggerClassName=navButton/>. Kept drawer + mobile Brands accordion + body scroll lock.
- Header.module.css: removed migrated .brandMega* styles; consolidated duplicate .drawerBrandsOpen.
- BrandMegaMenu.module.css: panel styling migrated from Header.
- SearchOverlay.tsx: added focus trap (Tab cycle) + focus return to trigger (previouslyFocused ref). Kept /search?q= submit. Header still owns body scroll lock (SearchOverlay does not touch body.overflow).
- ProductCard: UNCHANGED (max-height push-down already correct; no grid-rows rewrite).

VERIFICATION:
- lint exit 0, typecheck exit 0, build exit 0 (before and after deslop).
- SSR markup confirmed: 1 button trigger w/ aria, 1 nav#brand-mega panel, role=menu absent, All brands link present, 0 leftover brandMega classes.
- Architect reviewer: APPROVED — all 16 acceptance criteria pass; pointerdown/onClick race, Escape-reopen, fragment onBlur containment, inert tabbability all correct.

LEARNINGS:
- This is NOT a git repo; backups live in _backup/.
- Figma MCP was disconnected; UI built on existing globals.css tokens. Pixel-level Figma sync deferred to /visual-verdict when MCP reconnects.
- CSS-module class substring matching ([class*=brandMega]) counts container+inner+links, which can mislead a "duplicate modal" count.

=== FIGMA ALIGNMENT PASS (2026-05-23, MCP reconnected) ===
Figma MCP reconnected -> pulled frames 1:2 (home), 1:68 (brands hover), 1:146 (search), 1:216 (grid), 1:298 (card hover).
FINDINGS: existing build already matched Figma desktop on tokens (#fbfbfb/#191919/Pretendard/69px header), content-max 1000 + 140 gutter, 5-col brand panel x=140/358/564/751/977, product cards 320x400 / 3-col / pitch 340 (320+20), hover push-down ~22px (row1 bottom 494 -> row2 top 514), home video full-width 732px.
CONCRETE GAPS FIXED:
- Nav labels were Title Case; Figma shows lowercase across all 3 frames -> lowercased in Header.tsx (nav items + search/login/cart/menu) and mobile drawer (home/close/brands/all brands/login/cart) and BrandMegaMenu trigger ("brands") + panel link ("all brands"). "man" implemented as "men" per user.
- ProductGrid.module.css row-gap 16px -> 20px (gap: 16px 20px -> gap: 20px) to match Figma 20px vertical gap.
NO behavior change: Brands modal reducer {visible,pinned}, outside-click/Escape/focus-out, SearchOverlay focus trap/return all untouched.
VERIFIED: lint 0, typecheck 0, build 0. Fresh SSR: single #brand-mega panel, lowercase nav, 0 Title-case leftovers, 0 role=menu. Architect: APPROVED.
NOTE: card still shows brand line (Figma card-hover mock omits brand) — kept intentionally for usability; search overlay kept as compact 86px top bar (functional criteria met). Logo remains text "sable" (Figma uses a logo image asset; text is the MVP placeholder).

=== FIGMA FINISHING PASS + LOGO (2026-05-23) ===
Pre: git init + checkpoint commit fed8dcb "Checkpoint before Figma finishing pass" (.gitignore added; node_modules/.next/_backup excluded; 136 files tracked).
Pulled Figma screenshots 1:146 (search) + 1:298 (card hover) for pixel reference.
CHANGES:
- ProductCard.tsx/.module.css: removed brand line (Brand prop + <strong>); meta = product name (left) + price (right), per Figma card-hover. Dead .name span/strong rules removed; .name ellipsizes. max-height normal-flow push-down PRESERVED (not overlay). ProductGrid.tsx no longer passes brand (keeps getBrandById null-filter).
- Header.tsx/.module.css: replaced text "sable" logo with <img src="/public/sable-logo.svg" alt="sable" width104 height20> in header + drawer logo links (eslint-disable no-img-element, since next lint --max-warnings=0). Logo links to "/", centered in 69px header via flex. Removed dead logo text CSS (font-size/letter-spacing) incl. 767px overrides. alt provides text fallback.
- SearchOverlay.tsx/.module.css: submit "Search" -> magnifier inline SVG; close "X" -> X inline SVG (aria-labels kept). Pixel align: bar 86px white, full-width underline input left:70px right:104px bottom:24px, close icon top:22px right:70px, magnifier bottom:24px right:70px, backdrop rgba(0,0,0,0.5) inset 86px 0 0. Mobile gutters 20px/right:56px. /search?q= submit, focus trap, Escape, focus-return all UNCHANGED.
UNTOUCHED: BrandMegaMenu state model (hover/pin/close, outside-click, Escape, focus-out, single instance, <nav aria-label=Brands>, no role=menu), "men" not "man".
VERIFIED: lint 0, typecheck 0, build 0 (pre+post deslop). SSR: logo img from /sable-logo.svg alt=sable in href=/ link; product card meta = name+price, 0 brand <strong>. Architect: APPROVED. Deslop: no edits needed.
REMAINING VISUAL DIFFS (intentional): (1) product name rendered full-regular weight — Figma bolds the trailing token ("Nova") but that is not generalizable across product names, so kept uniform regular. (2) search overlay nav links are hidden while overlay open (our overlay is a focused dialog); Figma frame shows nav behind the bar — kept dialog focus model for a11y (focus trap). (3) home/listing imagery still PlaceholderImage (no real product photography in repo).

=== FIGMA GLOBAL VISUAL CONSISTENCY PASS (2026-05-24) ===
Header split into 3 groups inside .inner: logo / .mainNav(<div>, static) / .utilityNav(<div>, margin-left:auto). search/login/cart moved into .utilityNav (right-aligned to content edge). mainNav = BrandMegaMenu(first) + 8 navItems. mainNav is NOT <nav> and not positioned -> header has exactly 1 <nav> (the Brands panel); BrandMegaMenu absolute panel still anchors to .header (sticky). Tokens --nav-logo-gap:48 --nav-item-gap:28 --nav-util-gap:32. ~80px main<->utility gap = flex slack (no justify-content:space-between).
Container width single-sourced: --page-gutter redefined clamp->20px (per-side inset); shared rule `.page-content,.utility-page { width: min(--content-max, calc(100vw - 2*--page-gutter)); margin:0 auto }`. Replaced ALL literal calc(100vw-40px) (was 8 across globals/Header/BrandDirectory/BrandMegaMenu/ProductDetail) -> 0. Removed dead 1179 :root{--page-gutter:24px} and redundant 767 width overrides. Added --page-top:25, --utility-page-top:74, --utility-page-bottom:96 (tokenized rhythm).
/brands kept as DIRECTORY: outer container -> .page-content (shared width/top), BrandDirectory.module.css keeps its own 5-col grid (->3@1179 ->1@767); dropped only the redundant .directory width/margin. NOT collapsed.
Files changed: styles/globals.css, components/layout/Header.tsx, components/layout/Header.module.css, components/brand/BrandDirectory.tsx, components/brand/BrandDirectory.module.css, components/layout/BrandMegaMenu.module.css, components/product/ProductDetail.module.css.
PRESERVED (untouched logic): BrandMegaMenu.tsx (hover/pin/close/outside/Escape/focus-out, single instance, <nav aria-label=Brands>, no role=menu), SearchOverlay.tsx (/search?q=, focus trap, focus return), ProductCard.* (max-height push-down), "men", mobile drawer.
VERIFIED: lint 0, typecheck 0, build 0; literal count 0; no desktopNav/.directory refs; SSR header nav===1, role=menu 0, single #brand-mega, group order logo->mainNav->utilityNav; /brands page-content + 5-col grid + 29 links. Architect APPROVED. Deslop: no edits.
REMAINING FIGMA DIFFS (intentional): nav gaps tokenized to Figma ~28/32/48 but bold-on-hover (weight 700) shifts neighbors by design; product card name uniform regular (Figma bolds trailing token, not generalizable); imagery still PlaceholderImage; search overlay keeps focused-dialog model (nav hidden while open) for a11y.

=== HEADER VISUAL INVARIANT PASS (2026-05-24) ===
ROOT CAUSE: no scrollbar-gutter stabilization. Tall routes (catalog grids, home) show a vertical scrollbar; short routes (login/information/cart/checkout) do not. Header .inner is margin:0 auto (centered), so the logo/nav shifted horizontally ~half a scrollbar (~7-8px) between scrolling and non-scrolling routes. NOT an active-route style (none exists), NOT button baseline (button already shares .navButton). Secondary: hover font-weight:700 widened text and pushed neighbors (interaction-time shift).
FIX:
- styles/globals.css: html { scrollbar-gutter: stable } -> body content width constant on every route -> centered header no longer shifts. (the per-route fix)
- Header.module.css: explicit header text invariant on .navLink/.navButton/.mobileTextButton (margin,border,padding,background,color,cursor,font-family,font-size:14,font-weight:400,line-height:1,letter-spacing:0,text-transform:none,text-decoration:none) so global/page typography cannot vary header text. .logo/.drawerLogo height:100% + align-items:center + line-height:0 + flex-shrink:0 -> 20px logo mark vertically centered deterministically in 69px header. .logoMark display:block width:auto height:20px (fixed).
- Bold-width reserve: .navLink/.navButton -> inline-flex column, align-items:flex-start, with ::after{content:attr(data-label);height:0;overflow:hidden;font-weight:700;visibility:hidden} reserving the bold footprint. Text left edge stays put; hover bold grows into reserved width (absorbed by gap) -> no neighbor/baseline shift.
- Header.tsx: data-label on every nav Link/button (incl. cart with count). BrandMegaMenu.tsx: data-label="brands" on trigger ONLY (state model untouched).
FILES: styles/globals.css, components/layout/Header.module.css, components/layout/Header.tsx, components/layout/BrandMegaMenu.tsx.
VERIFIED: lint 0, typecheck 0, build 0; scrollbar-gutter:stable in built CSS; SSR <header> BYTE-IDENTICAL (4711 bytes) across / /men /search?q=test /checkout /styling /login /information /brands (/cart identical modulo count); all 12 data-label present; header nav===1, role=menu 0, single BrandMegaMenu, men kept. Architect APPROVED; deslop no edits.
REMAINING: computed-style measurement was inferred from byte-identical SSR + global CSS + scrollbar-gutter (no headless browser available to read getComputedStyle live). scrollbar-gutter:stable needs a modern browser (Chrome 94+/FF97+/Safari18.2+); very old browsers fall back to prior behavior (acceptable for MVP). Pretendard not bundled -> system fallback (same on all routes).
